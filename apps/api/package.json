{
  "name": "@monitoring/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/api",
    "build": "go build -o bin/api ./cmd/api",
    "typecheck": "go vet ./...",
    "test": "go test ./...",
    "db:migrate": "go run ./cmd/api -migrate",
    "db:seed": "go run ./cmd/seed"
  }
}
